# Python-focused MCP project template
project: my-project

servers:
  filesystem:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-filesystem"]
    env: {}
    tags: [core, filesystem]

  git:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-git"]
    env: {}
    tags: [core, vcs]
